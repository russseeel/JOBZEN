{
  "name": "job-board-backend",
  "version": "1.0.0",
  "description": "Backend API for Job Board application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"cyan,magenta\" \"nodemon server.js\" \"echo 'Frontend lancé sur le port 8080' && python3 -m http.server 8080 --directory ../frontend\"",
    "dev:backend": "nodemon server.js",
    "dev:frontend": "python3 -m http.server 8080 --directory ../frontend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "job-board",
    "api",
    "express",
    "nodejs"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.6.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.11"
  }
}
